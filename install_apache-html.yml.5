---

- hosts: all
  become: true
  tasks:

  - name: install httpd server and php
    yum:
      name:
        - httpd
        - php
      state: latest
      update_cache: yes
    when: ansible_distribution == "Fedora"

