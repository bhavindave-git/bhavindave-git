---

- hosts: all
  become: true
  tasks:

  - name: install httpd server and php
    package:
      name:
        - "{{ var_http_pkg }}"
        - "{{ var_php_pkg }}"
      state: latest
      update_cache: yes
    when: ansible_distribution == "Fedora"

